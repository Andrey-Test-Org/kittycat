# go-orders

Tiny orders service.

## Run

```
cd go-orders
go run .
```

## Endpoints

- `GET /orders` — list all orders
- `POST /orders` — create order
- `GET /order?id=...` — fetch by id
- `GET /health` — health check
